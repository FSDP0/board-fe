# Debug level : DEBUG
logging:
    level:
        org:
            springframework: INFO
server:
    port: 9000

api:
    # Local Environment : http://localhost:<Port Num.>/<Context Paths>
    url: http://localhost:8000/boards
